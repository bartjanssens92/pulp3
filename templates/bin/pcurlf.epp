<%- | String $username,
      String $password
| -%>
#!/bin/bash

ARGS=$1
name=$2

curl -s "http://<%= $username -%>:<%= $password -%>@localhost:8000/pulp/api/v3/${ARGS}/?name=${name}" | python -mjson.tool
