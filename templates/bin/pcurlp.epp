<%- | String $username,
      String $password
| -%>
#!/bin/bash

ENDPOINT=$1
DATA=$2

curl -s -L -X POST -H "Content-Type: application/json" "http://<%= $username -%>:<%= $password -%>@localhost:8000/pulp/api/v3/${ENDPOINT}/" -d "${DATA}" | python -mjson.tool
